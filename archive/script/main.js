"use strict"

/*
var years = prompt('Сколько вам лет?', 100);

alert('Вам ' + years + ' лет!');

var isAdmin = confirm("Вы - администратор?");
    if (!isAdmin)
        alert( 'Так а какого хуя тогда ты лезешь?' );
else
    alert('Hi!');
*/

alert(navigator.platform);